# T0 Foundation Record

- Status: completed
- Branch: `competition-integration`
- Frozen downstream base: `928da0d4a9c1a2c6a476a502623954a0f589dd1c`
- Planning parent: `6b9fb7e2884f61a078a52cdf7a0440a4d9f7df68`
- Package manager: npm 11.13.0
- Verified runtime: Node.js 24.16.0

The frozen downstream base is the foundation commit above. This report is a
separate metadata-only descendant so it can record the immutable SHA without a
self-reference. T1, T2, and T3 must all branch from the frozen downstream base,
not from this report commit or an inferred branch tip.

## Delivered

- Added the minimal React, TypeScript, and Vite application entry required to
  prove a production build.
- Added an npm lockfile and strict TypeScript configuration.
- Added the shared provider-neutral contract under
  `apps/web/src/contracts/**`.
- Made fixture provenance a discriminated type so fixture data cannot be typed
  as live data.
- Added UI-readable evidence, workflow, approval, simulated action,
  verification, and audit state to the snapshot contract.
- Added explicit `idempotencyKey` inputs and discriminated command results with
  stable transition, reference, and idempotency error codes.
- Added a read-only recovered-state contract example and one consistency test.
  The example is not a runtime fixture, provider, or state machine.

## Changed paths

- `.gitignore`
- `package.json`
- `package-lock.json`
- `tsconfig.json`
- `apps/web/index.html`
- `apps/web/src/main.tsx`
- `apps/web/src/pages/App.tsx`
- `apps/web/src/contracts/**`
- `reports/tasks/T0.md`

## Checks

| Command | Result | Evidence |
|---|---|---|
| `npm install --package-lock-only --offline --ignore-scripts --no-audit --no-fund` | passed | Lockfile resolved entirely from the local npm cache. |
| `npm ci --offline --ignore-scripts --no-audit --no-fund` | passed | 28 packages installed without network access or lifecycle scripts. |
| `npm run typecheck` | passed | Strict TypeScript check completed with no errors. |
| `npm run test` | passed | 1 contract consistency test passed. |
| `npm run build` | passed | Vite production build completed; output is ignored under `apps/web/dist/`. |
| `npm run check` | passed | Type check, test, and production build all passed in sequence. |
| `npm ls --depth=0` | passed | All direct dependencies resolved at their locked versions. |
| `git diff --cached --check` | passed | No whitespace errors in the frozen foundation commit. |

The first offline lockfile attempt included Vitest and failed because its
transitive metadata was not fully cached. Vitest was removed rather than
falling back to the network. Tests use Node's built-in runner with `tsx` for
TypeScript and TSX transformation.

## Deliberately not configured or implemented

- Lint and formatting commands are not configured; T0 adds no speculative
  linting or formatting dependency.
- The deterministic fixture engine, transitions, reset, evidence exporter, and
  golden-path tests belong to T1.
- The guided interface, presentation tests, browser flow, and visual evidence
  belong to T2.
- Submission assets and claims belong to T3.
- Runtime composition of the T1 data source and T2 presentation is deferred to
  T4 after their scoped commits are merged.
- This T0 task added no live provider, network request, server, database,
  process control, deployment, upload, or T1-T3 worktree.

## Remaining gaps

- `README.md` still describes the repository as planning-only. It is outside
  the T0 write boundary and must be corrected by the final integrator when the
  implemented state and verified claims are known.
- T0 proves only the skeleton and shared contract. It does not satisfy the PRD
  golden path or visual acceptance criteria on its own.
- During final verification, clean Orca child worktrees for `task/web-demo`
  and `task/submission-package` appeared at the frozen T0 base. Concurrent
  T2/T3 sessions manage them outside this T0 task; they are not T0 delivery
  evidence.
