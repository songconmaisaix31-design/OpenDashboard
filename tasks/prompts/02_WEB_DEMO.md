# Task Prompt — T2 Web Demo

Build only the guided presentation for the fixture journey, consuming the
frozen T0 contract.

Work only in the assigned worktree and branch created from the frozen T0 SHA.
You may change:

- `apps/web/src/components/**`
- `apps/web/src/pages/**`
- `apps/web/src/styles/**`
- `apps/web/src/assets/**`
- `apps/web/public/**`
- `apps/web/tests/presentation/**`
- `apps/web/tests/e2e/**`
- `reports/tasks/T2.md`

Do not change the domain, fixtures, state machine, shared contract, root
configuration, dependencies, or submission files. Keep fixture/mock
provenance, approval, and audit state visible. Do not use external assets,
network data, secret access, or live-provider behavior. Do not delegate, merge,
push, deploy, or upload.

Deliver the 90-second guided interface and representative desktop visual
evidence. Record changed files, checks, results, and gaps in
`reports/tasks/T2.md`. If a required change is outside the allowed paths,
record the blocker and stop that part of the task.
